/**
 * The model represents the logic an rules, specified by the store manager that indicate when
 * customers arrive at the store and how much time they spend while they are inside.
 */
package model;